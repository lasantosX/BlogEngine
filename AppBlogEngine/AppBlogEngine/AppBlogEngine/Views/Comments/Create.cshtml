@model AppBlogEngine.Models.Comments

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Comments</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group" style="display:none">
                <label asp-for="idPublished" class="control-label"></label>
                <input asp-for="idPublished" class="form-control" />
                <span asp-validation-for="idPublished" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="textComment" class="control-label"></label>
                <input asp-for="textComment" class="form-control" />
                <span asp-validation-for="textComment" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
