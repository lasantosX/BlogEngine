@model IEnumerable<AppBlogEngine.Models.Comments>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.idPublished)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.textComment)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.idPublished)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.textComment)
            </td>
           @* <td>
                <a asp-action="Edit" asp-route-id="@item.idComments">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idComments">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idComments">Delete</a>
            </td>*@
        </tr>
}
    </tbody>
</table>
